language: python
python:
  - "3.5"
install: "pip install -r requirements.txt"
script: "./src/manage.py test"
services:
  - postgresql
before_script:
  - psql -c 'create database keadatabase_test;' -U postgres
  - psql -U postgres -c "create extension postgis"
